# Variables de entorno

1. Copiar el archivo `.env.example`.
2. Renombrarlo a `.env`.
3. Completar las variables si es necesario.

Ejemplo:

cp .env.example .env

El proyecto utiliza dotenv para cargar las variables de entorno.